# Import variables set-up by dwl into the environment and notify systemd that
# dwl is ready.
exec test -n "$$NOTIFY_SOCKET" \
  && systemctl --user import-environment \
					 DISPLAY \
					 WAYLAND_DISPLAY \
					 XCURSOR_SIZE \
					 XCURSOR_THEME \
  && systemd-notify --ready
